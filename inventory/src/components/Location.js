import { Button } from "@mui/base";
import {
  AppBar,
  Card,
  CardActions,
  CardContent,
  Grid,
  IconButton,
  Toolbar,
  Typography,
} from "@mui/material";
import { Box } from "@mui/system";
import React from "react";
import MenuIcon from "@mui/icons-material/Menu";

export const Location = () => {
  return (
    <>
      <div>
        A summary is a brief statement or restatement of main points, especially
        as a conclusion to a work: a summary of a chapter. A brief is a detailed
        outline, by heads and subheads, of a discourse (usually legal) to be
        completed: a brief for an argument
      </div>
    </>
  );
};
